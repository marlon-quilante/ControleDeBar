@model EditarGarcomViewModel

@{
    ViewBag.Titulo = "Edição de Garçom";
}

<html>
<body>
    <main>
        <form class="col-4 needs-validation" action="/Garcom/Editar/@Model.Id" method="post" novalidate>
            <div class="mb-3">
                <label for="Nome" class="form-label">Nome:</label>
                <input name="Nome" id="Nome" type="text" class="form-control" value="@Model.Nome" required>
                <div class="invalid-feedback">
                    Informe o nome do garçom!
                </div>
            </div>
            <div class="mb-3">
                <label for="CPF" class="form-label">CPF:</label>
                <input name="CPF" id="CPF" type="text" class="form-control" value="@Model.CPF" required>
                <div class="invalid-feedback">
                    Informe o CPF do garçom!
                </div>
            </div>

            <div class="d-flex justify-content-end">
                <a class="btn btn-secondary px-4 me-1" asp-action="Index">Voltar</a>
                <button class="btn btn-success px-4 me-1" type="submit">Salvar</button>
            </div>
        </form>
    </main>

    @* Script de validação com Bootstrap *@
    <script>
        (() => {
            'use strict'

            const forms = document.querySelectorAll('.needs-validation')

            Array.from(forms).forEach(form => {
                form.addEventListener('submit', event => {
                    if (!form.checkValidity()){
                        event.preventDefault()
                        event.stopPropagation()
                    }

                    form.classList.add('was-validated')
                }, false)
            })
        })()
    </script>
</body>
</html>